{
  "name": "char-motion",
  "private": true,
  "workspaces": [
    "packages/*",
    "website"
  ],
  "scripts": {
    "build:react": "npm run build --workspace=@char-motion/react",
    "dev": "npm run dev --workspace=website",
    "vercel-build": "npm run build --workspace=@char-motion/react && npm run build --workspace=website"
  }
}
